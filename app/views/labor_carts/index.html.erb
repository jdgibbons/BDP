<h1>Listing labor_carts</h1>

<table>
  <thead>
    <tr>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @labor_carts.each do |labor_cart| %>
      <tr>
        <td><%= link_to 'Show', labor_cart %></td>
        <td><%= link_to 'Edit', edit_labor_cart_path(labor_cart) %></td>
        <td><%= link_to 'Destroy', labor_cart, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Labor cart', new_labor_cart_path %>
