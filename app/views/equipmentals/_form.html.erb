<%= form_for(@equipmental) do |f| %>
  <% if @equipmental.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@equipmental.errors.count, "error") %> prohibited this equipmental from being saved:</h2>

      <ul>
      <% @equipmental.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :rate %><br>
    <%= f.text_field :rate %>
  </div>
  <div class="field">
    <%= f.label :units %><br>
    <%= f.text_field :units %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
